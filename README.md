# Scientific-GPT_Pre-Training-Only
I built a GPT model entirely from scratch to master the architecture of Large Language Models. Instead of using pre-made checkpoints, I engineered the full pipeline myself, using custom crawlers to gather specialized scientific data. I managed the heavy computation by setting up a Multi-GPU system with CUDA and PyTorch. 
